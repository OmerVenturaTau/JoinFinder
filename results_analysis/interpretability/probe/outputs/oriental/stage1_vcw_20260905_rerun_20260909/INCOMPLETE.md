# Partial verification rerun

Tile, glyph, and word completed and reproduce the corresponding files in
`../stage1_vcw_20260905_manuscript_balanced/` byte for byte. The shared-space
CPU fit was stopped because the complete equivalent run already exists there;
this directory is not a standalone complete result. Use the earlier directory's
`summary.json` and `metrics_by_space.csv` for the complete four-space result.
