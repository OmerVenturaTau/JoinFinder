# utilities/CharacterModule

Character-level OCR feature utilities.

`alto_features.py` computes character/glyph statistics from ALTO XML. These
features are separate from the active neural glyph branch in `models/glyph_branch.py`
but may be useful for diagnostics or tabular analysis.
